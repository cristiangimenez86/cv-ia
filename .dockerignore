# Shared context for frontend + backend images (docker build -f … from repo root).
# Do NOT list `backend` or `frontend` here — both Dockerfiles COPY those trees.
.git
.github
**/.git

backend/tests
infra
openspec
docs
*.md

# Context bloat (deps stage reinstalls from package-lock)
frontend/node_modules
frontend/.next

# Other roots
**/bin
**/obj
**/.vs
